export interface Book {
  id: number;          // Identificador único do livro
  name: string;       // Nome do livro
  subtitle: string;   // Subtítulo do livro
  image: string;      // URL da imagem do livro
  price: number;      // Preço do livro
}